@model ClothsListViewModel
@*<div class="row">

        <h4>Current Sex: @Model.CurrentSex</h4>
        <h4>Current Catagory: @Model.CurrentCatagory</h4></div>
        @foreach (var cloth in Model.Cloths)
        {
            var photo = "~/images/" + (cloth.ImageUrl ?? "Elves_Purple.jpg");

            <div class="card m-3" style="min-width:16rem; max-width:30.5%;">
                <div class="card-header">
                    <h5>@cloth.Name</h5>

                </div>
                <img class="card-img-top cus imageThumbnail" src="@photo" asp-append-version="true" />
                @*<div class="card-footer">
                        <a asp-controller="home" asp-action="Details" asp-route-id="@cloth.Id" class="btn btn-primary m-1">View</a>
                        <a asp-controller="home" asp-action="Edit" asp-route-id="@cloth.Id" class="btn btn-primary m-1">Edit</a>
                        <a href="#" class="btn btn-danger m-1">Delete</a>
                    </div>
            </div>

        }
    </div>*@
    <div class="row marginTop">
        @foreach (Cloth cloth in Model.Cloths)
        {
            @await Html.PartialAsync("ClothsSummary", cloth);
            @*var photo = "~/images/" + (cloth.ImageUrl ?? "Elves_Purple.jpg");
            <div class="col-sm-4 col-lg-4 col-md-4" style="border-bottom-width:thick">
                <div class="thumbnail">

                    <img src="@photo" alt="" asp-append-version="true" />
                    <div class="caption">
                        <h5 class="pull-right">@cloth.Price.ToString("c")</h5>
                        <h5>
                            <a asp-controller="Cloth" asp-action="Details" asp-route-drinkId="@cloth.ClothId">@cloth.Name</a>
                        </h5>*@
            @*<p>@cloth.Description</p>*@
            @*</div>
                    <div class="addToCart text-md-center">
                        <p class="button ">
                            <a class="btn btn-success" id="cartButton" asp-controller="ShoppingCart" asp-action="AddToShoppingCart" asp-route-drinkId="@cloth.ClothId">
                                Add to cart
                            </a>
                        </p>
                    </div>
                </div>
            </div>*@
        }
    </div>
